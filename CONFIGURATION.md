# Configuration

## Environment file

Copy `.env.example` to `.env` and edit values as needed.

## Important notes

- This repository excludes model weights and private datasets from source control.
- Do not configure live broker credentials in the repository.
